import { CategoryProductPipe } from './category-product.pipe';

describe('CategoryProductPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryProductPipe();
    expect(pipe).toBeTruthy();
  });
});
